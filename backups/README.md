# Backups

Datostemplede prosjekt-snapshots lagres her før større endringer eller publisering.

Snapshotene inneholder kildekode, public-filer og prosjektkonfigurasjon. Genererte mapper (`node_modules`, `.next`, `.vercel`) og lokale miljøhemmeligheter (`.env.local`) skal ikke kopieres hit.
